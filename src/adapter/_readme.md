# adapter
DDD adapter layer — bridges domain logic to the outside world.

- `application/` — Application services, use cases, and DTOs.
- `infrastructure/` — Infrastructure services: database access (D1), KV, R2, external APIs, etc.
